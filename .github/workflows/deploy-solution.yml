name: Deploy Contoso_University_Core

on:
  workflow_run:
    workflows: ["Pack Contoso_University_Core"]
    branches: [main]
    types:
      - completed
  workflow_dispatch:
<<<<<<< HEAD

jobs:
  deploy:
    runs-on: windows-latest
    environment: production
    steps:
      - uses: dawidd6/action-download-artifact@v2
        with:
          workflow: pack-solution.yml
          name: packed-solution
      - uses: microsoft/powerplatform-actions/import-solution@v0
        with:
          environment-url: https://org668c5352.crm6.dynamics.com/
          user-name: ${{ secrets.USERNAME }}
          password-secret: ${{ secrets.PASSWORD }}
          solution-file: contoso_university_core_managed.zip
=======
    jobs:
        deploy:
          runs-on: windows-latest
          environment: production
          steps:
            - uses: dawidd6/action-download-artifact@v2
              with:
                workflow: pack-solution.yml
                name: packed-solution
            - uses: microsoft/powerplatform-actions/import-solution@v0
              with:
                environment-url: https://org668c5352.crm6.dynamics.com
                user-name: ${{ secrets.USERNAME }}
                password-secret: ${{ secrets.PASSWORD }}
                solution-file: contoso_university_core_managed.zip
      
>>>>>>> 1ab8172689118db31e0f0a7c15b3a8e2b5645ca2
